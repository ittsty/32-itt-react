###Dev jouney###

>หน้า home เริ่มจากทำหัวข้อเปล่าๆ >>ทำ navbar ทิ้งไว้ >> อยากได้ปุ่ม >> ต่อไปอยากได้ตารางก็เริ่มทำตารางเปล่าๆมาก่อน >> ทำแบบเดียวกันไว้สองชุด อันนึงเอาไว้ใช้สำหรับ user ส่วนอีกอันก็เอามาใส่ inputfield กับปุ่มไว้เพิ่มข้อมูล 
> เริ่มแต่งสไตล์ให้จนครบ >> เริ่มทำเชื่อมกับ db เอามาแสดงในตารางก่อน >> ทำให้กรอกข้อมูลแล้วขึ้นไปอยู่บน db >> ทำให้ลบข้อมูลได้ >> ทำหน้า owner สุดท้าย
